from setuptools import setup, find_packages

setup(
    name='hymn-lists',
    version='0.1.0',
    packages=find_packages(),
    description='Hymnal index data',
    author='mdgrossi',
    url='https://github.com/musicministry/song-urls',
    python_requires='>=3.7',
)